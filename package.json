{
  "scripts": {
    "generate": "pnpm -r prisma generate",
    "build": "pnpm -r build"
  },
  "dependencies": {
    "@credit-system/blend-point": "workspace:*",
    "@credit-system/credit-system": "workspace:*"
  },
  "devDependencies": {
    "@credit-system/blend-point": "workspace:*",
    "@credit-system/credit-system": "workspace:*"
  }
}
